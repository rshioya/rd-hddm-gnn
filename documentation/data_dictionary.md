# Data dictionary

For every public CSV, NPZ, NPY, PT, mesh, and adjacency file, provide: relative path, description, columns or tensor keys, shape, dtype, units, normalization, geometry family, split, seed, node ordering, and missing-value convention.

Use `documentation/data_dictionary_template.csv` as the machine-readable index.
